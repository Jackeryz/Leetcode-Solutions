
        num = int(res)
        
        if f:
            return 0
        if res == '':

                break
            else:
                res += c
            if '0' <= c <= '9':
        for c in s:
